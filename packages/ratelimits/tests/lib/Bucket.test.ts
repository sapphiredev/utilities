import { Bucket } from '../../src';

describe('Bucket', () => {
	test('Common', () => {
		expect(typeof Bucket).toBe('function');
	});

	test('Unlimited', () => {
		// TODO(kyranet): fill in tests
	});

	test('Limited', () => {
		// TODO(kyranet): fill in tests
	});
});
